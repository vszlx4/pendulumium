>>> May 13th, 2026.
    when the seed changes, the next state (number) also changes,
    adding 2 bits in "self.state = x^2" would make the results sometimes irregular and not mathematically deterministic.
    as for now, I have no idea about absolute randomness.

>>> May 14th, 2026.
    the idea changed, I switched to a UUID generator,
    it uses timestamp + process ID (pid), counter (it reduced collisions while rapid generating), and entropy.
    both combined makes a 128bit UUID that can be easily indexed and sorted.
    tested the generator on a 10 million rapid generations, and I had 0 duplications.

>>> May 15th, 2026.
    dumpeddddddd...
    128bits seems a bit off, I'll see if I can make it UUID-friendly,
    otherwise I'll just make it 256bits or 512bits.

>>> May 16th, 2026. Part 1
    later on this day I'll put more details here

>>> May 16th, 2026. Part 2
    now the generator makes a UUIDv7 compliant identifiers,
    I'll split the additional features into three major updates,
    probably will start with decoder, to_datetime, and batch functions now.